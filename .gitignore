# dependencies
node_modules

# build output
.next
dist
.turbo
*.tsbuildinfo
next-env.d.ts

# env
.env
.env.local
.env.*.local

# personal music dropped into the library locally: never commit copyrighted audio or its metadata
fixtures/tracks/baby/

# generated / large media (regenerate with `bun run fixtures`)
evidence/**/*.mp4
evidence/**/*.webm
evidence/**/*.wav
fixtures/tracks/**/*.wav

# os
.DS_Store

# local tooling (machine-specific launch config, cloudflared quick-tunnel logs)
.claude/
.tunnels/

# playwright
apps/web/test-results
apps/web/playwright-report
apps/web/blob-report
apps/web/playwright/.cache
